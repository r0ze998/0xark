# 0xARK UI/UX review — 2026-09-05

対象: `main` の `19872c4` を起点とした `solana/client/`。
設計・実装にGPT-6 Astraを使用。既存のSprite Seas方針を継承。

## 判断

市販ゲームと遜色ない品質は、配色変更だけでは達成できない。0xARKでは
「自分のカードを選ぶ」「戦術を理解する」「封印後の選択が守られる」
という中核操作と、読み込み失敗・支払い確認の扱いが先に必要だった。
本変更はその最初の実装であり、商用品質に到達したという認定ではない。

現在の実装ではSTEALは無効。PROMOTEは同じカードを昇格する。
所有履歴と通常のvaultの接続にはREADME記載の未解決点がある。
UIはこの現状を表示し、過去のピッチを実装済み機能として扱わない。
戦闘計算、コントラクト、ZK回路、経済条件は本変更の対象外。

## 実装した変更

| 場面 | 変更 | プレイヤーへの効果 |
|---|---|---|
| 接続前 | 実在するカタログカードとプレイ手順を提示。接続と有料署名を区別 | 遊ぶ内容を把握してから接続できる |
| ホーム | 所持カードのショーケース、BATTLE主導線、補助のVAULT/SHOP/TRADE | 次の行動と自分のコレクションが分かる |
| コレクション | 所持/全種/未所持と陣営の複合フィルタ、5列、キーボード操作 | 目的のカードを探しやすい |
| 対戦待機 | 自分のカードを確認、カード不足/energy不足の理由、接続エラーのretry | 意図せずデモ対戦が始まらない |
| 対戦準備 | 連続選択で空枠へ配置、明示交換、Action説明と選択枚数 | 5枚を自然に組み、戦術を理解できる |
| 封印 | 固定snapshot、編集中断、タイマ停止、失敗時の選択保持 | 証明と手札の食い違いを防ぐ |
| 時間切れ | 選択済みカードとActionを保持し空枠だけ補完 | 自分の判断が勝手に消えない |
| Energy補充 | 未取得の区別、確認中の二重実行防止、Cancel/Escape、focus維持 | 状態と費用を把握して操作できる |
| 結果 | 無効な奪取ボーナス表示を抑止、最終HP/BP表示を反映 | 現在のルールと画面の結果が一致する |
| 読み込み | 架空のvault/dayを生成しない。home操作前に読み込み | 通信障害を実データと誤認しない |

## 参考にした一次資料と設計上の推論

- [MTG Arena公式](https://magic.wizards.com/en/mtgarena):
  プレイモードと使用デッキの選択。0xARKではカードと次の行動を優先する。
- [MARVEL SNAP公式・Retreat](https://marvelsnap.helpshift.com/hc/en/3-marvel-snap/faq/29-what-does-retreating-do/):
  退却時の損失を盤面上のCube表示と対応させる。
  0xARKでも費用や不可逆な結果を実行操作の近くで説明する。
- [MARVEL SNAP公式・Upgrade](https://marvelsnap.helpshift.com/hc/en/3-marvel-snap/faq/34-what-does-upgrading-a-card-do/?p=android):
  カードの視覚変化とコレクション進行の接続。
  今後のPROMOTE/ENGRAVE演出はカード個体の継続性を中心に評価する。
- [Cartridge公式・Toast](https://docs.cartridge.gg/controller/toast-notifications):
  pending/confirming/errorを区別する通知。0xARKへの適用は状態表示の原則であり、
  基盤の移行やセッション署名の導入を実施したものではない。

これらは操作原則の参考資料。実機を並べた視覚比較や、競合との品質同等性を
検証した資料ではない。

## 検証とレビュー範囲

- 新規回帰テスト7件: 連続配置、明示交換、時間切れの保持、snapshot不変性、
  energy未取得・再生境界・上限・未取得時の補充無効化。
- 既存Nodeカードエンジン53件・戦闘メカニクス49件・damage-calc41件。
  これら既存テストはクライアントの全対戦フローを保証するものではない。
- クライアント47モジュールの構文、Sprite Seas design-lint、差分の空白チェック。
- コードレビューで、補充確認後の古い状態による再描画と、切断後の失敗した
  commitmentの残存を追加修正。
- ブラウザの見た目確認、実ウォレット、2人対戦、通信遅延下のE2Eは未実施。
  Rust/回路は変更していないため、この作業ではAnchorの再ビルドを行っていない。

## 商用品質を判断するために残る確認

1. **画面と入力**: 1024×576の基準表示、ノートPC、横向きスマホで文字、
   カード、ダイアログが切れず、キーボードとタッチで主要操作を完遂できること。
   現在のDESIGNに従い縦画面は回転案内を維持する。
2. **対戦と通信**: 2人で各roundを進め、署名拒否、遅延、切断、再接続でも
   選択・確定状態が一致すること。既存のセッション復旧設計も別途検証が必要。
3. **演出と理解**: SEAL/REVEAL/ENGRAVE/PROMOTEの待機と確定が分かり、
   短縮操作・reduced motionでも結果を理解できること。
4. **カード詳細の真実性**: provenance取得失敗と0勝の分離、通常vaultと個体mintの
   接続、昇格の前後表示は追加改善が必要。データ基盤の未実装を演出で補わない。
5. **初見プレイ**: 説明なしで最初の手札を組み、Actionの効果と敗因を説明できるか
   観察する。迷った操作・離脱箇所に基づいて次の改善を選ぶ。

奪取の有効化や所有権モデルの変更は、ゲーム仕様とチェーン実装の別の判断を伴う。
このPRはその判断を先取りしない。
