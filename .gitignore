node_modules/
dist/
target/
*.log
logs/
.env
.DS_Store
contracts/manifests/
.vite
*.bak
solana/client/index.html.bak
solana/client/pirates-tilemap-b64.txt

# Internal dev notes — not for public reviewers
CLAUDE.md
TASKS.md

# Old Starknet/Dojo prototype — superseded by Solana implementation
# (anchored to repo root; must not match nested solana/client/)
/client/

# GDD archive — keep only current GDD.md
GDD-v0.2.md
GDD-v0.3.md
logs/

# Local validator ledgers. /test-ledger holds the parked F1 manual-e2e
# fixtures (17 DuelStates, fixture wallet vaults) — it is irreplaceable
# evidence, not build output. Ignoring it is not a backup, but it does stop
# a plain `git clean -fd` at the repo root from deleting it.
/test-ledger/
