# Wave 1–4 Screens — Archived (Post-hackathon)

These 16 screen components were built during Waves 1–4 of the UI sprint.
They are **not wired** to the active `solana/client/app.js` and are deferred to Season 2.

See `docs/POST_HACKATHON_ROADMAP.md` for the re-integration plan.

The active Phase 15 battle flow uses `solana/client/src/components/` instead.
